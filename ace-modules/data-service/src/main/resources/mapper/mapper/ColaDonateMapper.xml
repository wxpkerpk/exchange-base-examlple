<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitcola.dataservice.mapper.ColaDonateMapper">

    <insert id="insert">
        insert into ag_admin_v1.cola_community_donate (id,user_id,type,number,time,coin_code)
        values (#{id},#{userId},#{type},#{number},#{time},#{coinCode})
    </insert>

</mapper>